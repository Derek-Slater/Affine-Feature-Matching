This is where output files should go. They should also be .pngs so they aren't tracked by Git.

This file is here so the `Output/` directory is tracked - otherwise, Git doesn't track empty directories.
